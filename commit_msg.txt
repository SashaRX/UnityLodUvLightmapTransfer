v0.9.96 — one-to-one source dedup for same-src overlap fix

Phase 2 split into 2a (match) + 2b (dedup) + Phase 3 (transfer):

- Extract FindBestSourceShell helper with excludeSources param
- Extract DetectMergedShell helper for UV0 coverage check
- Phase 2a: match all target shells to best source (as before)
- Phase 2b: detect when multiple non-merged targets claim same source
  - keep best match (lowest avg3D), evict others
  - re-match evicted targets excluding claimed sources (up to 3 iterations)
  - unresolvable targets forced to merged mode
- Phase 3: transfer UV2 using final deduplicated assignments

Fixes same-src overlap on tiling UV0 geometry (e.g. LOD1 overlapping
shells src89, src92, src102, src104 each claimed by 2-3 target shells).
TransferResult.dedupConflicts tracks reassignment count.
